Use Git Add" to track the file

Use "touch" to creat file

Use "git rm -- cached Gitcommand1 to committed (Unstage) 

Use "git commit -m "initial Commit" to use commit

Use "git add ." to use all files in track/stage

Note : based on Git add command is  helping to  staging the file & Git commit -m/ git commit .a command is initing the file.

